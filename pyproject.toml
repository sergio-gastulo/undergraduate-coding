[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "utils"
version = "0.1.0"
dependencies = [
    "nbformat>=5.10.4",
    "plotly>=6.8.0",
]

[tool.setuptools.packages.find]
where = ["py_v"]
include = ["utils*"]
